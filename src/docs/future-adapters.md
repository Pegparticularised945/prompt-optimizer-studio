# Future Adapters Backlog

## Browser Worker Direction

Planned later, not part of the current CPAMC/API implementation:

- Add a `BrowserWorkerAdapter` for OpenClaw / Gemini Web / Antigravity.
- Reuse the existing queue, convergence, judge, and job detail system.
- Treat browser automation as an additional execution channel, not a replacement for the current CPAMC path.
- Prefer manually logged-in browser profiles over automated login to reduce anti-bot and account-risk issues.
- Preserve the `pooled-3x` conversation policy for stateful browser channels.
