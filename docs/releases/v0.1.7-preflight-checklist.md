# V0.1.7 发布前最终 Checklist

> 目标：发布 **`v0.1.7`**，不使用 `hotfix` 作为对外版本名。

## A. 代码与产物边界

- [ ] 当前待发布分支内容已确认无误。
- [ ] `package.json` 版本号为 `0.1.7`。
- [ ] 发布文案统一使用 `v0.1.7`，不出现 `hotfix`。
- [ ] 不包含 self-bootstrap / Track 3 / lane / recovery 等私有实验资产。
- [ ] 不包含 authoritative compare winner 口径。
- [ ] `.ops/*.db*` 已被忽略，不会进入发布产物。
- [ ] 确认没有本地 SQLite、预览数据库、个人路径文件被纳入 commit / tag / release。

## B. 自动化验证

在待发布提交上执行：

```bash
npm run check:architecture
npm run typecheck
npm test
npm run build
```

逐项确认：

- [ ] `npm run check:architecture` 通过。
- [ ] `npm run typecheck` 通过。
- [ ] `npm test` 通过。
- [ ] `npm run build` 通过。

## C. 手动 smoke

### 1. 首页 / 创建任务

- [ ] 首页可正常打开。
- [ ] 能正常创建一个新任务。
- [ ] 任务创建后不会卡在明显错误的初始状态。

### 2. 任务详情 / 结果台

- [ ] 轮次卡片显示 `上轮提示词评分` 等新文案。
- [ ] 已完成任务不会继续显示“这版要到下一轮才会评分”。
- [ ] 占位式 `Run a single sample` / `single-sample` MVE 已被人话兜底。
- [ ] 主视图不再直接裸露原始 `stream error / INTERNAL_ERROR / 504` 文案。

### 3. 长期规则 / 待生效引导

- [ ] 自动运行中的任务详情里可见 `调整长期规则`。
- [ ] 可以从待生效引导生成长期规则草稿。
- [ ] 保存长期规则后，提示文案与行为一致。

### 4. 停止逻辑

- [ ] 连续 3 轮达到阈值后，任务会正确停止。
- [ ] 若第 3 次过线但同轮没生成新 output，也能正确 completed，而不是错误 failed。

## D. 发布材料

- [ ] `docs/releases/v0.1.7-changelog.md` 已确认。
- [ ] `docs/releases/v0.1.7-github-release.md` 已确认。
- [ ] GitHub Release 标题使用：`v0.1.7 - 结果台与结构治理更新`。
- [ ] Release 文案中明确说明：本次不包含 authoritative compare winner。
- [ ] Release 文案中明确说明：不包含本地数据。
- [ ] 如需致谢，已注明吸收 PR #6 的结构治理方向。

## E. Git 与 GitHub 操作

- [ ] 待发布分支工作区为 clean。
- [ ] 目标 commit 已确认。
- [ ] 如需先合并到 `main`，已完成合并并再次检查工作区 clean。
- [ ] 创建 tag：`v0.1.7`
- [ ] 推送分支与 tag。
- [ ] 在 GitHub 上创建 Release Draft。
- [ ] 将 `docs/releases/v0.1.7-github-release.md` 内容粘贴到 Release 描述。
- [ ] 检查 Release 页面标题、tag 和正文无误后，再点击发布。

## F. 发布后回看

- [ ] GitHub Release 页面显示的版本名正确为 `v0.1.7`。
- [ ] 发布页不包含 `hotfix` 命名。
- [ ] 发布页不包含 compare winner 误导口径。
- [ ] 发布页不包含任何本地数据或私人路径。
- [ ] 如需发更新帖，内容与 Release 文案保持一致。
